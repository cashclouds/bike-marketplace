# ⚡ БЫСТРАЯ ИНСТРУКЦИЯ: Обновление Vercel

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

1. Открыть Vercel Dashboard
2. Обновить переменные окружения
3. Перестроить приложение

---

## 📱 ШАГИ

### Шаг 1: Откройте Vercel Dashboard

Перейдите по ссылке:
```
https://vercel.com/dashboard
```

### Шаг 2: Выберите проект "bike-marketplace"

Нажмите на проект в списке.

### Шаг 3: Перейдите в Settings → Environment Variables

Найдите вкладку **Settings** → **Environment Variables**

### Шаг 4: Обновите переменные

#### Для фронтенда (Next.js):

Проверьте наличие:

| Ключ | Значение | Примечание |
|------|----------|-----------|
| `NEXT_PUBLIC_API_URL` | `https://your-api-domain.com/api` | Замените на ваш API домен |
| `NODE_ENV` | `production` | Уже должно быть |

#### Для бэкенда (если на Vercel):

| Ключ | Значение | Примечание |
|------|----------|-----------|
| `NODE_ENV` | `production` | - |
| `USE_HTTPS` | `true` | - |
| `JWT_SECRET` | `8375892f2ee3b1bf0e7c07fea3116f683b876542ee05d71cf75880318f685411` | Сгенерированный ключ |
| `DB_HOST` | `your-db-host` | Ваш PostgreSQL хост |
| `DB_PORT` | `5432` | - |
| `DB_NAME` | `bike_marketplace_prod` | - |
| `DB_USER` | `postgres` | - |
| `DB_PASSWORD` | `your_strong_password` | ⚠️ Сильный пароль! |
| `FRONTEND_URL` | `https://bike-marketplace-rho.vercel.app` | - |
| `CLOUDINARY_CLOUD_NAME` | `your-cloud-name` | - |
| `CLOUDINARY_API_KEY` | `your-api-key` | - |
| `CLOUDINARY_API_SECRET` | `your-api-secret` | - |
| `STRIPE_PUBLIC_KEY` | `pk_live_...` | Stripe public key |
| `STRIPE_SECRET_KEY` | `sk_live_...` | Stripe secret key |

### Шаг 5: Сохраните изменения

Нажмите кнопку **Save** или **Add** для каждой переменной.

### Шаг 6: Перестройте приложение

#### Вариант A: Автоматический rebuild

Просто выполните push на main:
```bash
git push origin main
```

Vercel автоматически перестроит приложение.

#### Вариант B: Ручной rebuild

1. На странице проекта в Vercel
2. Нажмите кнопку **Deploy** или **Redeploy**
3. Выберите ветку **main**
4. Нажмите **Deploy**

---

## ✅ ПРОВЕРКА

После rebuild проверьте:

### 1. Приложение загружается
```
https://bike-marketplace-rho.vercel.app/
```
Должна быть главная страница.

### 2. Тесты логирования

a) Откройте Chrome DevTools (F12)
b) Перейдите на страницу логина
c) Попробуйте залогиниться
d) Проверьте что нет ошибок в консоли

### 3. Проверите Network вкладку

a) Откройте Network вкладку (F12 → Network)
b) Залогиньтесь
c) Ищите запрос к API
d) В Response headers должно быть:
```
Set-Cookie: accessToken=...; HttpOnly; Secure; SameSite=Strict
Set-Cookie: refreshToken=...; HttpOnly; Secure; SameSite=Strict
```

---

## 🆘 ЕСЛИ НЕ РАБОТАЕТ

### Проблема: Приложение не перестраивается

**Решение:**
1. Дождитесь завершения предыдущего build
2. Нажмите кнопку **Redeploy**
3. Проверьте логи в разделе **Deployments**

### Проблема: Ошибка "API не найден"

**Решение:**
1. Проверьте что `NEXT_PUBLIC_API_URL` правильный
2. Убедитесь что API сервер работает
3. Проверьте CORS настройки

### Проблема: "Cannot find module 'cookie-parser'"

**Решение:**
1. Это значит что backend не переустановил зависимости
2. Выполните `npm install` в папке backend
3. Пуште на GitHub
4. Перестройте на Vercel

---

## 📝 ВАЖНЫЕ ЗАМЕТКИ

⚠️ **БЕЗОПАСНОСТЬ**:
- ❌ Никогда не коммитьте `.env` файлы в GitHub
- ✅ Используйте Vercel Dashboard для переменных окружения
- ✅ Генерируйте сильные пароли и ключи

📚 **ДОКУМЕНТАЦИЯ**:
- Полная инструкция: `PRODUCTION_SECURITY_CHECKLIST.md`
- Подробный гайд: `VERCEL_DEPLOYMENT_GUIDE.md`

🔄 **АВТОМАТИЧЕСКИЙ REBUILD**:
- Каждый push на `main` автоматически перестраивает приложение
- Проверьте статус в разделе **Deployments** на Vercel Dashboard

---

## ✨ ГОТОВО!

После выполнения этих шагов приложение будет обновлено с:
- ✅ httpOnly cookies
- ✅ CSRF protection
- ✅ Security улучшениями
- ✅ Bug fixes

Приложение доступно по адресу:
```
https://bike-marketplace-rho.vercel.app/
```

**Дата**: 2025-11-25
**Версия**: 1.0
